---
description: "Implement existing OpenSpec change (skip architect phase)"
agent: build
---

ultrawork Implement the specified OpenSpec change. tasks.md already exists — skip architect phase.

Start from Phase 2 (coder) through Phase 5 (docs + summary).
Read handoff.md between phases. Use s-* subagents.
