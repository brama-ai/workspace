apiVersion: v1
kind: Service
metadata:
  name: core
  namespace: brama
  labels:
    app.kubernetes.io/part-of: brama
    app.kubernetes.io/component: core
    app.kubernetes.io/name: core
spec:
  selector:
    app.kubernetes.io/name: core
  ports:
    - name: http
      port: 80
      targetPort: 80
  type: ClusterIP
