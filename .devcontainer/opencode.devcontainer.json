{
  "$schema": "https://opencode.ai/config.json",
  "permission": "allow",
  "mcp": {
    "ssh": {
      "type": "local",
      "command": ["npx", "-y", "mcp-server-ssh"],
      "enabled": true
    }
  }
}
