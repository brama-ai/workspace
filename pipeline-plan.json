{
  "profile": "standard",
  "reasoning": "OpenSpec tasks.md exists with 9 remaining tasks (tests, docs, quality checks). All test and doc files already exist from prior failed runs — coder needs to verify/fix them, validator runs static analysis, tester confirms tests pass. No architect needed (tasks.md ready). Documenter not needed (docs already written by coder). Single app (core), no migrations, no API changes.",
  "agents": ["coder", "validator", "tester", "summarizer"],
  "skip_openspec": true,
  "estimated_files": 8,
  "apps_affected": ["core"],
  "needs_migration": false,
  "needs_api_change": false,
  "is_agent_task": false,
  "is_bug": false,
  "bug_severity": null,
  "timeout_overrides": {},
  "model_overrides": {}
}
